language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t dennisli0705/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dennisli0705/docker-react npm run test